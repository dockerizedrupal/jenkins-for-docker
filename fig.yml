jenkins:
  image: simpledrupalcloud/jenkins:latest
  hostname: jenkins
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - jenkinsdata
  environment:
    - SERVER_NAME=localhost
jenkinsdata:
  image: simpledrupalcloud/data:latest
  hostname: jenkinsdata
  volumes:
    - /jenkins
