jenkins:
  image: simpledrupalcloud/jenkins:dev
  hostname: jenkins
  ports:
    - "80:80"
    - "443:443"
  volumes_from:
    - jenkinsdata
jenkinsdata:
  image: simpledrupalcloud/data:dev
  hostname: jenkinsdata
  volumes:
    - /jenkins
